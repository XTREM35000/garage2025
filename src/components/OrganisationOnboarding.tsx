import React from 'react';

// Simplified component to avoid complex type issues
const OrganisationOnboarding: React.FC = () => {
  return (
    <div>
      <p>Organisation onboarding temporarily simplified</p>
    </div>
  );
};

export default OrganisationOnboarding;